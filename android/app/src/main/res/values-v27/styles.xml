<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The cutout API exists only on Android 8.1/API 27 and later. -->
    <!-- Qualified styles replace their unqualified definitions. Keep the
         explicit parents and core items here so dot-name inference cannot fall
         back to AppTheme and accidentally restore its native ActionBar. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/ic_launcher_background</item>
        <item name="android:windowLayoutInDisplayCutoutMode">always</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor">#0D110E</item>
        <item name="android:navigationBarColor">#0D110E</item>
        <item name="android:windowLayoutInDisplayCutoutMode">always</item>
    </style>
</resources>
